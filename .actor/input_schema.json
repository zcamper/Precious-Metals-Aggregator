{
    "title": "Precious Metals Aggregator Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "search_terms": {
            "title": "Search Terms",
            "type": "array",
            "description": "Search terms to find products across all dealers (e.g., 'Silver eagle', 'gold coin').",
            "editor": "stringList",
            "items": { "type": "string" },
            "prefill": ["Silver coin"]
        },
        "max_items_per_dealer": {
            "title": "Max Items Per Dealer",
            "type": "integer",
            "description": "Maximum number of products to scrape from each dealer.",
            "default": 5,
            "minimum": 1,
            "maximum": 100,
            "editor": "number"
        },
        "dealers": {
            "title": "Dealers to Scrape",
            "type": "array",
            "description": "Which dealers to include. Leave empty to scrape all 11 dealers.",
            "editor": "stringList",
            "items": { "type": "string" },
            "prefill": []
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Select proxy to use for scraping.",
            "editor": "proxy",
            "prefill": { "useApifyProxy": true },
            "default": { "useApifyProxy": true }
        }
    },
    "required": []
}
